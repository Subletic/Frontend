stages:
  - build
  - verification

build:
  stage: build
  image: node:16-alpine
  before_script:
    - npm install
  script:
    - npm run build


lint:
  stage: verification
  image: node:16-alpine
  script:
    - npm ci
    - npm run lint
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules
    policy: pull

test:
  stage: verification
  image: node:16-alpine
  script:
    - npm ci
    - ng test --watch=false
