stages:
  - lint

lint:
  stage: lint
  image: trion/ng-cli:16.0.1
  script:
    - npm ci
    - ng add @angular-eslint/schematics --skip-confirmation
    - npm run lint
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules
    policy: pull
